#include<stdio.h>
void swap(int x,int y);
int main()
{
  int a=18,b=45;
  swap(a,b);
  return 0;
}
void swap(int x,int y)
{
  int t;
  t=x;
  x=y;
  y=t;
  printf("inside function is %d and %d",x,y);
}
